{
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "prestart": "pkill -f node || true && npm run build",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
